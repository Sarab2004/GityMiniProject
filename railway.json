{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "/bin/sh -c \"python manage.py migrate && python manage.py collectstatic --noinput && python create_superuser.py && python manage.py seed_demo && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT --forwarded-allow-ips '*'\"",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
